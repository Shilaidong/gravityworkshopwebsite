.DS_Store
**/.DS_Store
node_modules/
.next/

# Screen recordings (too large for git)
*.mov
*.mp4
*.MOV

# Local analysis scratch
.ref-oryzo-frames/
.claude/

# 出图原始素材（不部署，仅供重新调色）
art-src/

# 根目录的测试夹具：必须与 index.html 同源才能跑，用完可删，
# 随时从 tools/ 再复制一份。前导斜杠是必须的——不加的话
# tools/ 里的夹具母本也会被一起忽略掉。
/_shot.html
/_probe.html

# 截图与探针输出
.shots/
__pycache__/
